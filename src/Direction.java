/*
 * Direction is an enumerated type that represents possible directions of rotation in the xy-plane. Any given shape can either
 * rotate clockwise (Direction.CLOCKWISE) or counterclockwise (Direction.COUNTERCLOCKWISE).
 */
public enum Direction {
	CLOCKWISE, COUNTERCLOCKWISE
}
